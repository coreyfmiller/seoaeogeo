export const metadata = {
  title: "Keyword Arena V2 | Duelly",
  description: "AI-powered keyword battle arena. Same scoring as Pro Audit.",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
